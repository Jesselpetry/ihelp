# Changelog

ทุกการเปลี่ยนแปลงที่มีผลต่อผู้ใช้ของโปรเจกต์นี้จะถูกบันทึกไว้ในไฟล์นี้

รูปแบบอ้างอิงจาก [Keep a Changelog](https://keepachangelog.com/en/1.1.0/)
และเลขเวอร์ชันอ้างอิงจาก [Semantic Versioning](https://semver.org/lang/th/)
(`MAJOR.MINOR.PATCH` — MAJOR = breaking change, MINOR = ฟีเจอร์ใหม่, PATCH = แก้บั๊ก/ปรับเล็กน้อย)

วันที่ของทุกเวอร์ชันเขียนด้วยรูปแบบไทยและปีพุทธศักราช (เช่น `30 สิงหาคม 2569`)
ซึ่งต่างจาก ISO 8601 ที่ Keep a Changelog กำหนดไว้ — ตั้งใจให้อ่านง่ายสำหรับผู้ใช้
ของเว็บนี้ที่เป็นนักศึกษาไทย หัวข้อย่อยใช้ **เพิ่ม** / **ปรับปรุง** / **แก้ไข** /
**ความปลอดภัย** แทน Added / Changed / Fixed / Security

## [Unreleased]

ยังไม่มีรายการรอปล่อยในรอบถัดไป

## [0.8.0] - 22 กันยายน 2569

**ผู้ร่วมพัฒนา:** Chatan Petry ([@Jesselpetry](https://github.com/Jesselpetry))

### เพิ่ม

- **สื่อแต่ละชิ้นมีข้อมูลกำกับรุ่นและสัปดาห์** — `SubjectAsset` เพิ่มฟิลด์
  `isCurrentYear`, `isDuplicate`, `duplicateOf`, `status`, `edition` และ `week`
  ทำให้ ICS จับคู่สไลด์บรรยายกับใบงานแล็บของสัปดาห์เดียวกันได้ และกันไฟล์รุ่นเก่า
  หรือไฟล์ที่บีบอัดซ้ำออกจากรายการหลักแทนที่จะโชว์ปนกันหมด

### ปรับปรุง

- **หน้ารายวิชาเริ่มด้วย "วิชานี้คืออะไร" ไม่ใช่ "ต้องทำอะไรบ้าง"** — การ์ด
  ภาพรวมย้ายขึ้นมาอยู่ใต้ชื่อวิชาแทนที่จะอยู่ท้ายหน้า คนที่เพิ่งคลิกเข้ามาจึงได้
  บริบทก่อนเจอรายการงาน
- **ยุบการ์ดภาพรวมสองใบให้เหลือใบเดียวแบบแท็บ** — "ภาพรวมและโครงสร้างรายวิชา"
  กับ "ภาพรวมรายวิชาอย่างเป็นทางการ" เคยเป็นการ์ดคนละใบ หัวข้อคล้ายกัน กินพื้นที่
  คนละหน้าจอ ตอนนี้อยู่ในกรอบเดียวกันใต้แท็บ "สรุปเข้าใจง่าย" / "ข้อมูลทางการจากคณะ"
- **"ขั้นที่ 1–4" เปลี่ยนเป็นหมวดหมู่ตามจุดประสงค์** — ปูพื้นฐานเนื้อหา ·
  ย่อก่อนเข้าห้องสอบ · ตะลุยโจทย์และแบบฝึกหัด · จำลองสอบจริงและซ่อมจุดอ่อน
  ลำดับเดิมยังอยู่ (และยังเป็นลำดับที่แนะนำ) แต่ไม่อ่านเป็นด่านที่ต้องผ่านทีละด่าน
  สำหรับคนที่เข้ามาเพื่อทำโจทย์อย่างเดียว
- **โมดูลที่ยังไม่เปิดถูกพับเก็บ** — จากเดิมที่กุญแจล็อคแทรกอยู่กลางรายการที่ใช้ได้
  จริง ตอนนี้รวมไว้ท้ายหมวดใต้ปุ่ม "กำลังจะเปิด N รายการ" เปิดดูได้ถ้าอยากรู้ว่ามี
  อะไรกำลังมา
- **ตัวกรองช่วงสอบเลิกโชว์ตัวเลขที่บวกกันไม่ได้** — เดิมปุ่มเขียน "ทั้งหมด 11 ·
  ก่อนมิดเทอม 11 · หลังมิดเทอม 6" ซึ่งชวนให้ลบกันแต่ลบไม่ได้ (โมดูลที่ใช้ทั้งเทอม
  ถูกนับซ้ำทั้งสองฝั่ง) ตอนนี้ตัวควบคุมย้ายขึ้นไปอยู่เหนือเนื้อหาที่มันกรอง เต็มความกว้าง
  และมีบรรทัดเดียวบอกว่ากำลังแสดงอะไรอยู่
- **การ์ดโมดูลกะทัดรัดขึ้นและเรียงสองคอลัมน์บนจอ ≥640px** — สแกนด้วยตาได้เร็วขึ้น
  โดยไม่ต้องเลื่อนผ่านแถวเต็มความกว้างสิบเอ็ดแถว
- **รายการเอกสารเป็นการ์ดบนจอเล็ก ตารางบนจอ ≥640px** — เดิมหน้ารายวิชามีตาราง
  กว้างชุดเดียว บนมือถือจึงต้องเลื่อนแนวนอนอ่าน ตอนนี้ `ModuleIndex`,
  `ModuleReader` และตารางเอกสารประจำคาบเรนเดอร์เป็นการ์ดต่ำกว่า `sm:`
  และกลับเป็นตารางตั้งแต่ `sm:` ขึ้นไป
- **ชั้นวางปักหมุดของ ICS เปลี่ยนจากกริดการ์ดเป็นตารางจัดกลุ่มรายสัปดาห์** —
  ใช้โครงเดียวกับตารางคลังทรัพยากร (หัวตารางร่วม คอลัมน์ปุ่มแบบ sticky)
  หนึ่ง `<tbody>` ต่อหนึ่งสัปดาห์ พร้อมแถวหัวกลุ่มที่บอกป้ายสัปดาห์ ชื่อหัวข้อ
  และช่วงสอบ และเริ่มต้นแบบพับไว้ เพราะชั้นวางนี้เป็นทางลัด ไม่ใช่เนื้อหาหลักของหน้า
- **สแกนรุ่น v2 เป็นไฟล์หลักของ ICS สัปดาห์ 1–3** — ไฟล์ชุดเดิมขอบหน้าถูกตัด
  `RESOURCES_MANIFEST.md` จึงย้ายไฟล์เดิมไปสถานะ "เก่า" และยกฉบับ v2 ขึ้นเป็นฉบับหลัก
- **คลังทรัพยากรเปิดให้ใช้งานจริงแล้ว** — `/library` เสิร์ฟสารบัญจริง
  `/library/[slug]` prerender ทุกเอกสารอีกครั้ง ทั้งสอง route กลับเข้า sitemap
  และไดอะล็อกต้อนรับเอาป้าย "เร็วๆ นี้" ออก ธง `LIBRARY_COMING_SOON` กับคอมโพเนนต์
  หน้าคลุมถูกลบทิ้ง ไม่ได้แค่ปิดสวิตช์ จึงไม่มีหน้าคลุมค้างรอให้เผลอเปิดกลับมา
- **ปิดการแชร์ไฟล์เข้าคลังของผู้ใช้ไว้ก่อน** — ธงใหม่ `UPLOADS_ENABLED` ปิดเป็น
  ค่าเริ่มต้น เปิดได้ด้วย `NEXT_PUBLIC_UPLOADS_ENABLED=1` เท่านั้น `/upload` ตอบ 404
  เมนูบัญชีซ่อนปุ่มแชร์ และ `createResource` ปฏิเสธการเขียนในตัวเอง ฟอร์มเก่าที่ค้าง
  อยู่หรือ POST ที่ยิงตรงจึงไม่ผ่านเช่นกัน — การอัปโหลดรูปโปรไฟล์ไม่ได้รับผลกระทบ

## [0.7.0] - 22 กันยายน 2569

**ผู้ร่วมพัฒนา:** Chatan Petry ([@Jesselpetry](https://github.com/Jesselpetry))

### ความปลอดภัย

- **ข้อสอบเก่าในคลังทรัพยากรจำกัดสิทธิ์เฉพาะ insider** — ไฟล์ข้อสอบ 169 ไฟล์
  (10 วิชา) ย้ายจาก bucket สาธารณะไปอยู่ใน `ihelp-library-exams` แบบปิด เสิร์ฟผ่าน
  signed URL อายุ 10 นาที หลังผ่าน `isInsider()` แล้วเท่านั้น URL สาธารณะเดิม
  ตอบ 400 แล้ว การจัดประเภทดูจาก `category` ไม่ใช่ path — หน้าข้อสอบที่ถ่ายเก็บไว้
  ใน `ics/pages/` อยู่นอกโฟลเดอร์ `exams/` ถ้าใช้ path ตัดสินจะหลุดสาธารณะ
- **ชื่อไฟล์และ metadata ของสื่อไม่หลุดไป browser อีกต่อไป** — เดิม client
  component import จาก `lib/subject-library.ts` ซึ่ง import manifest ที่ระดับ
  module ทำให้ชื่อไฟล์ทุกชิ้นรวมข้อสอบเก่าติดไปใน JS bundle ตอนนี้ค่าที่ UI ใช้
  ย้ายไป `lib/subject-library-ui.ts` ที่ไม่ import ข้อมูลใด ๆ
- **ถอดรหัสผ่าน iJudge ที่ฝังในสคริปต์ออกจาก repo** — credential ของ iJudge
  เคยติดอยู่ในประวัติ git ของ repo นี้ ตอนนี้อ่านจาก environment อย่างเดียว
  ไม่มีค่าสำรองในโค้ด และเลิก track `data/html_cache` (7.2 MB ที่แอปไม่เคยอ่าน)

### เพิ่ม

- **ติดตั้งเป็นแอปได้ (PWA)** — เพิ่ม `app/manifest.ts`, ไอคอนครบชุด (192/512,
  รุ่น maskable สำหรับ Android), `app/apple-icon.png` และ service worker
  (`public/sw.js`) ที่ cache เฉพาะ build output กับไอคอนแบบ content-hashed
  ส่วนการเปิดหน้าเป็นแบบ network-first เสมอ ไม่มีข้อมูลไหนถูกแคชแบบเก่าค้าง
  มีหน้าออฟไลน์ภาษาไทยสำรองไว้เมื่อเน็ตหลุด `proxy.ts` กันไม่ให้ session ของ
  Supabase มาแทรกการเรียก `manifest.webmanifest`
- **หน้ารวมเอกสารก่อนเข้าอ่าน (module index)** — โมดูลที่มีเอกสารตั้งแต่ 4 ชิ้นขึ้นไป
  (เช่นหน้า `/map` ของ ICS ที่มี 8 เอกสาร) จะเปิดมาที่หน้ารวมก่อน แทนที่จะพา
  ผู้อ่านดิ่งเข้าเอกสารแรกทันที มีมุมมองตารางและกริด ค้นหาได้ และบัตรแต่ละใบ
  นับจำนวนวิดีโอ/ไฟล์/หัวข้อ **จากเนื้อหาจริงในไฟล์ Markdown** ไม่ใช่ตัวเลขที่พิมพ์
  แยกไว้ต่างหาก — กันปัญหาบัตรโฆษณาว่ามี 4 คลิปแต่เปิดแล้วมีจริง 2
- **จำได้ว่าคาบไหนเปิดอ่านไปแล้ว** — ติ๊กถูกที่บัตรของเอกสารที่เคยเปิด เก็บด้วย
  `localStorage` ต่อเครื่อง ไม่ผูกบัญชี ใช้คำว่า "เปิดแล้ว" ไม่ใช่ "อ่านแล้ว" เพราะ
  เบราว์เซอร์รู้แค่ว่าเปิดหน้า ไม่รู้ว่าอ่านจริงหรือเข้าใจหรือเปล่า
- **เปิดดูไฟล์ (PDF/รูป) แบบ preview ในหน้าได้เลย** — ตารางเอกสารประจำคาบใน
  หน้า `/map` ของ ICS ตอนนี้จับคู่ชื่อไฟล์ในเนื้อหากับรายการสื่อจริงของวิชา
  (`assetsForCourse()`) กดดูตัวอย่างหรือดาวน์โหลดได้ทันทีโดยไม่ต้องออกไปหน้า
  คลังเรียนรู้ **ข้อสอบเก่ายังคงถูกกันออกจากชุดนี้เหมือนเดิม** —
  `assetsForCourse()` กรอง `isRestrictedAsset()` ทิ้งก่อนส่งให้ UI เสมอ
- **`RESOURCES_MANIFEST.md` ของ ICS** — ทะเบียนถาวรของไฟล์ทั้ง 120 ชิ้นในชั้นวาง
  แยกสาย A/สาย B ของสไลด์และแล็บทุกไฟล์ (ยืนยันด้วยการเรนเดอร์หน้าปกจริง ไม่ได้เดาจากชื่อไฟล์)
  พร้อมตารางไฟล์ที่เนื้อหาซ้ำกัน 8 กลุ่ม, ไฟล์ 7 ชิ้นที่ชื่อไม่ตรงเนื้อหา และรายการ
  งานนักศึกษาที่มีรหัสนักศึกษาของผู้อื่น
- **`scripts/ics-fix-misnamed-assets.sh`** — สคริปต์ dry-run/apply สำหรับแก้ชื่อไฟล์
  7 ชิ้นนั้น พร้อมรายการ object ค้างใน bucket ที่ต้องลบตามหลัง
- **หน้าคาบเรียนรายสัปดาห์ของ ICS — เรียนจบได้ในหน้าเดียว** — `/courses/06016411-Intro-to-Computer-Systems/map`
  แตกเป็น 8 เอกสาร: ภาพรวม + คาบที่ 1–7 แต่ละคาบมีเป้าหมายการเรียน เครื่องเล่นวิดีโอ
  แบบฝังพร้อมแท็บสลับคลิป รายการเอกสาร อุปกรณ์ที่ต้องเตรียม สรุปว่าใบงานสั่งทำอะไร
  และข้อควรระวังเฉพาะคาบ (เช่น ใบงานคาบ 4 มีรูปกับข้อความสั่งไม่ตรงกัน และคำบรรยาย
  รูปในคาบ 5 เรียก 7401 ว่า NOR ทั้งที่เป็น NAND แบบ open-collector)
  ปุ่มคาบก่อนหน้า/ถัดไปและสารบัญด้านข้างมาจาก `ModuleReader` เดิม ไม่ได้เพิ่ม route ใหม่
- **Markdown รองรับบล็อก ```youtube** — ฝังวิดีโอ YouTube ในเอกสารรายวิชาได้แล้ว
  สัดส่วน 16:9 ปรับตามจอ มีแท็บเลือกคลิปเมื่อมีหลายวิดีโอ ใส่ `title`/`aria-label`
  ให้ทุก iframe และโหลดแบบ lazy ใช้โดเมน `youtube-nocookie.com`
  รหัสวิดีโอถูกตรวจรูปแบบก่อนนำไปประกอบเป็น URL และบล็อกที่รูปแบบผิดจะแสดงเป็น
  กล่องโค้ดตามเดิมแทนที่จะขึ้นเครื่องเล่นเปล่า
- **ICS ได้ครึ่งหลังของวิชาคืนมา — สื่อ 17 ไฟล์ + วิดีโอ 35 คลิป** — สไลด์บรรยาย
  Chapter 01–07, ใบงานแล็บ 1–6, ประมวลการสอน 1/2569, ตำรา v0.5 และรายการอุปกรณ์
  จากเว็บของ ผศ.ดร.สุภกิจ นุตยะสกุล เข้าคลังของ `06016411` แล้ว พร้อมหน้าใหม่
  `/courses/06016411-Intro-to-Computer-Systems/map` ที่รวมตารางเรียน 7 คาบ
  ใบงานแล็บรายสัปดาห์ และลิงก์วิดีโอบรรยาย/สาธิตแล็บครบ 35 คลิป
  ทำให้ ICS ขยับจาก 5/11 เป็น **6/11** โมดูล
  ประมวลการสอนฉบับนี้ยืนยันว่า ICS **แบ่งผู้สอนคนละครึ่งเทอม** (ครั้งที่ 1–7
  ดิจิทัลลอจิก / ครั้งที่ 8–15 ฮาร์ดแวร์คอมพิวเตอร์) ซึ่งขัดกับ `summary.md`
  หัวข้อ 2 ที่เขียนว่าสไลด์ชุดหลังเป็น "คลังปีก่อน" ที่ใช้เป็นเนื้อหาเสริม
  — ยังไม่ได้แก้ `summary.md` เพราะเป็นไฟล์ที่ดูแลด้วยมือ
- **คลังเรียนรู้ต้องเข้าสู่ระบบก่อน** — `/courses/<วิชา>/library` เปิดให้เฉพาะ
  นักศึกษาคณะ IT ที่ล็อกอินด้วยอีเมล `@kmitl.ac.th` แล้วเท่านั้น ยังไม่ล็อกอิน
  จะเห็นหน้าให้เข้าสู่ระบบแทน กันจริงฝั่ง server — หน้านี้ไม่มีข้อมูลไฟล์อยู่เลย
  รายการสื่อมาจาก `/api/library/assets` ที่ตอบ 401 ถ้าไม่มี session
  ตรงตามที่ `summary.md` ของ FE และ ITF กำกับไว้ว่าห้ามเผยแพร่สาธารณะ
- **`npm run assets:sync`** — อัปโหลดไฟล์ใน `public/assets/` ขึ้น Supabase Storage
  อัปเฉพาะไฟล์ที่ยังไม่มี และแยกข้อสอบเก่าเข้า bucket แบบปิดให้อัตโนมัติ
- **ป้าย "แนะนำ" และ "submission.md" ในแถวตารางโจทย์** — พร้อมปุ่มสร้าง
  submission.md ในแถวของโจทย์ Learning Log ป้าย submission.md เปลี่ยนเป็นสีเขียว
  เมื่อไฟล์ถูก sync เข้า repo แล้ว
- **โจทย์สัปดาห์ 8–9 เข้าสู่ระบบการเรียนรู้ครบวงจร** — เพิ่มเฉลยอ้างอิงที่ตรวจแล้ว
  ให้โจทย์ใหม่ทั้ง 27 ข้อ (id 3290–3301, 3349–3363) ทำให้ `/pscp` แสดง concept tag
  จริงจากโค้ด และตัวตรวจในเบราว์เซอร์มี edge case แบบ differential (95 เคสใน 20 ข้อ)
- **โจทย์แนะนำเพิ่มเป็น 14 ข้อ** — BigFrame, Left Arrow, กองชาม, Shorten เข้ามาเป็น
  โมดูลเต็มใน `/recommended` (คู่มือ 7 หัวข้อ 2 ภาษา + เฉลย + แบบทดสอบ 6 ข้อ/โจทย์ +
  เคสสำหรับ grader)
- **คำใบ้เฉพาะโจทย์ในหน้าสร้าง submission.md** — โจทย์ Learning Log หลังกลางภาค 6 ข้อ
  มีแผงคำใบ้พับได้ที่ขั้น "ความเข้าใจโจทย์" และ "แผนแรก" (ยังต้องเขียนด้วยคำพูดตนเอง)
- **ตัวตรวจ `content:check` ครอบคลุมคลังโจทย์แนะนำ** — ทุกโฟลเดอร์ต้องมีไฟล์ครบและมี
  entry ในทุก registry ทั้งสี่ และแบบทดสอบทุกชุดต้องผ่าน `checkBank` เหมือน quiz หลัก
- **`scripts/verify_solutions.py`** — ตัวตรวจเฉลยอ้างอิงที่ใช้ได้ทั้งคลัง แทน
  `scripts/wk89_solutions/` เดิมที่เก็บเฉลยซ้ำอีกชุดและรันได้จากรากโปรเจกต์
  กับโจทย์แค่สัปดาห์เดียว ตัวใหม่หาพาธเอง รับ id, ช่วง id หรือ `--week`
  เฉลยผ่านตัวอย่างทางการ 93 จาก 94 ข้อ และรายงานข้อ `oj3362` ที่ตัวอย่างขัดกันเอง
  แยกไว้แทนที่จะทำให้ทั้งรอบล้ม พร้อมขึ้นทะเบียนสคริปต์ Python เป็น `pscp:*`
  และ `compro:*` ใน `package.json`

### ปรับปรุง

- **สื่อการเรียนย้ายออกจาก repo ไปอยู่บน Supabase Storage** — ไฟล์ 670 ชิ้น
  (~908 MB) ไม่ได้อยู่ใน git อีกต่อไป `public/assets/` ถูก gitignore แล้ว
  การ clone repo จึงไม่ได้สื่อการเรียนติดไปด้วย
- **หน้ารายการโจทย์ PSCP เปิดมาเป็นมุมมองตาราง** เป็นค่าเริ่มต้น (เดิมเป็นการ์ด)
  และเอาคอลัมน์ "แนวคิด" ออกเพื่อให้แถวไม่ล้นจอ
- **เรียงโจทย์แบบ Learning Log ขึ้นก่อน** แล้วตามด้วยสัปดาห์ล่าสุด
- **หน้าสรุปวิชา PSCP §3–§4 ปรับใหม่** — คลังโจทย์เป็น 135 ข้อ สัปดาห์ 1–9
  พร้อมชุดวันหมดเขตจริงของสัปดาห์ 8–9 และแยกรายการ Learning Log ก่อน/หลังกลางภาค
- **`build_pscp_registry.py` ลด noise ของ concept tag** — โจทย์ที่ยังไม่มีเฉลย
  (stub) จะได้ tag `["io"]` + ธง `tagsProvisional` แทนการเดามั่วจากคำในโจทย์
- **ระดับความยากแสดงเป็นดาว 5 ดวง** แทนแถบ ใช้ทั้งมุมมองการ์ดและตาราง
- **แถบนำทางเป็นแบบลอยขอบมนทุกหน้า**
- **ปุ่มลัดในแถวตารางโจทย์แสดงตลอด** (ทำเครื่องหมายผ่าน, ดูแบบเร็ว, เปิด iJudge)
  ไม่ต้องเอาเมาส์ไปชี้
- **ย้ายโดเมนไป `ihelp.chatan.in.th` พร้อมเปลี่ยน metadata ของเว็บ** — เว็บโตเกิน
  จุดเริ่มต้นที่เป็นเว็บวิชา PSCP ไปแล้ว (ตอนนี้มี PSCP, ITF, ICS, MFIT, คอมโปร, เคมี)
  `SITE_URL` ใน layout/robots/sitemap และค่า `og:url` แบบเต็ม 2 จุดชี้โดเมนใหม่
  ส่วน `pscp.chatan.in.th` redirect 308 ตามพาธเดิม ลิงก์เก่าจึงยังใช้ได้
  metadata รากขึ้นต้นด้วยชื่อแบรนด์ และ `app/page.tsx` เลิก override `description`
  ของตัวเอง ซึ่งเป็นต้นเหตุที่ `og:description` กับ `<meta name="description">`
  ไม่ตรงกัน — หน้าใต้ `/pscp` และ `/recommended` ยังใช้ metadata แบบ PSCP ตามเดิม
  เพราะเป็นเนื้อหาของ PSCP จริง ๆ
- **`sync_oj_problems.py` ถูกยุบไปใช้ scraper ของ repo คลังแทน** — สคริปต์ในนี้
  เหมือนกันทุกไบต์กับของ repo คลัง และเขียนลงพาธเดียวกันไม่ว่าจะรันจากที่ไหน
  งานทั้งหมดของมันเป็นส่วนย่อยของ scraper ฝั่งคลังที่รันด้วย `--fast`
  (ยืนยันว่าได้ผลลัพธ์เหมือนกันทุกไบต์ครบทั้ง 135 รายการ)
- **`build_pscp_registry.py` อ่านเฉลยอ้างอิงจาก worktree `.pscp-archive`** —
  branch หลักของ repo คลังเก็บโจทย์สัปดาห์ 8–9 (3290–3301, 3349–3363) เป็น
  `main.py` เปล่าไว้ให้ฝึกทำมือ ส่วนเฉลยจริงอยู่บน branch `solutions/2026-s1`
  ตัวสร้าง registry จึงอ่านจาก worktree นั้นเพื่อให้ยังมีเฉลยอ้างอิงครบ 135 ข้อ
  แทนที่จะเหลือช่องว่าง 63 ข้อ และถ้าไม่มี worktree จะ fallback ไปที่รากคลังตามเดิม
  clone ใหม่จึง build ผ่าน
- **อัปเดตสถิติโจทย์ OJ รอบใหม่** — จำนวนคนผ่าน จำนวนครั้งที่ส่ง เปอร์เซ็นต์
  และสถานะที่พลิกตามมา ใน `data/oj_problems.json` และ `data/all_problems_detail.json`

### แก้ไข

- **ชื่อใบงานแล็บสาย A ของ ICS เลื่อนไปหนึ่งช่อง** — `ics-lab-01` ถึง `ics-lab-04`
  ถูกตั้งชื่อไทยผิดมาตลอด (ใบที่ 1 เขียนว่า "ประตูลอจิกพื้นฐาน" ซึ่งเป็นหัวข้อของใบที่ 2)
  แก้ให้ตรงกับหัวกระดาษจริงแล้วทั้งสี่ใบ: 1 = รู้จัก Logisim · 2 = การทำงานของลอจิกเกท ·
  3 = พีชคณิตบูลีน · 4 = วงจรเชิงผสมเบื้องต้น
- **หัวตารางหน้าโจทย์ PSCP ล้นจอบนมือถือ** — แถบสถิติวางเป็น flex แถวเดียวที่ปล่อยให้
  ตัดบรรทัดเอง มาตรวัดความคืบหน้าสามอันจึงถูกบีบเบียดกันจนต่ำกว่า `min-w-28`
  และบรรทัด sync GitHub (`shrink-0`) ล้นออกนอกการ์ด ตอนนี้ต่ำกว่า `sm` เรียงเป็น
  แนวตั้ง มาตรวัดได้ `basis-full` ชื่อ repo ตัดด้วย truncate ปุ่มเชื่อมต่อเป็นปุ่ม
  เต็มความกว้าง และหัวข้อลดเหลือ `text-xl`
- **build บน Vercel ล้มเพราะเนื้อที่หมด (ENOSPC)** — file tracer ของ Turbopack
  ลาก ทั้งโปรเจกต์ (รวม `.git`) เข้า bundle เพราะมี `fs` call บน path ที่
  resolve ไม่ได้ตอน build ตอนนี้ override ทุกตัวถูก gate ด้วย `NODE_ENV` แล้ว
  trace ของ route `sitemap` ลดจาก 938 MB เหลือ 14.8 MB และ build output
  ลดจาก 1047 MB เหลือ 155 MB
- **สี pink ในตัวไฮไลต์โค้ด Python** ใช้ PSCP Pink (`--primary`) ให้ตรงกันทั้งเว็บ
  keyword ใช้ `--primary` ตรง ๆ และ operator เปลี่ยนเป็นสีม่วงตามที่ comment ระบุ
- **concept tag ของโจทย์สัปดาห์ 8–9** เดิมเพี้ยน (`strings`/`conditionals` เกือบทุกข้อ)
  เพราะไม่มีเฉลยให้ตัววิเคราะห์ AST อ่าน ตอนนี้มาจากเฉลยจริงแล้ว

## [0.6.0] - 30 สิงหาคม 2569

**ผู้ร่วมพัฒนา:** Chatan Petry ([@Jesselpetry](https://github.com/Jesselpetry))

### เพิ่ม

- **บัญชีผู้ใช้และการยืนยันตัวตนนักศึกษา IT** — เข้าสู่ระบบด้วย Google บัญชี
  `@kmitl.ac.th` ระบบตรวจรหัสนักศึกษา 8 หลักและรหัสคณะ (หลักที่ 3–4 ต้องเป็น `07`)
  บัญชีที่ไม่ผ่านจะถูกออกจากระบบทันทีและแจ้งว่า "เฉพาะนักศึกษาคณะ IT เท่านั้น"
- **โปรไฟล์สาธารณะ `/profile/[รหัสนักศึกษา]`** — ชื่อเล่น ชื่อจริง สาขา ลิงก์
  Facebook/Instagram และรายการทรัพยากรทั้งหมดที่เจ้าของโปรไฟล์แชร์เข้าคลัง
- **รูปโปรไฟล์** — อัปโหลดเองพร้อมเครื่องมือครอบรูปและบีบอัดในเบราว์เซอร์
  (ย่อเป็น WebP 512px) หรือใช้รูปจากบัญชี Google เป็นค่าเริ่มต้น
- **หน้าตั้งค่าโปรไฟล์ครั้งแรก `/onboarding`** — เติมชื่อ-นามสกุลอัตโนมัติจาก
  ทะเบียนนักศึกษาที่ใช้ร่วมกับระบบ ITGG
- **แชร์เข้าคลัง `/upload`** — อัปโหลดสไลด์ สรุป หรือโน้ต ระบุวิชา ประเภท และ
  ช่วงสอบ (กลางภาค/ปลายภาค) ไฟล์เก็บใน Supabase Storage และให้เครดิตผู้แชร์
- **คลังข้อสอบเก่า `/exams`** — จำกัดสิทธิ์เฉพาะบทบาท insider/admin ไฟล์อยู่ใน
  ที่เก็บแบบปิด เข้าถึงผ่านลิงก์ที่หมดอายุใน 10 นาที
- **ตัวนับผู้เข้าชมที่ footer** — วันนี้ / 7 วัน / ทั้งหมด นับตามวันเวลาไทย
  เก็บเป็นค่าแฮชที่เปลี่ยนทุกวัน ไม่เก็บ IP และไม่ใช้คุกกี้ติดตาม
- **ไดอะล็อกต้อนรับแบบเลือกปลายทาง** — ถามผู้ใช้ใหม่ว่าอยากเริ่มที่ห้องแลป PSCP
  หรือคลังทรัพยากร
- **ฮับวิชา EN-KMITL (คอมโปร)** — หน้าสารบัญก่อนเริ่มทำแบบทดสอบ ปุ่มกระโดดไปข้อที่ต้องการ
  พร้อมสถานะตอบแล้ว/ถูก/ผิด และข้อสอบจำลอง 60 ข้อ รวมกับ 10 ข้อคัดสรรเป็น 70 ข้อ
- **หมวดวิชา IT-KMITL** — ฮับ ICS/Digital Logic (สรุป, แบบทดสอบ 50 ข้อ, ข้อสอบฝึกพร้อม
  เฉลยละเอียด, บทวิเคราะห์โครงข้อสอบ, คลังทรัพยากร) และฮับ MFIT คณิตศาสตร์สำหรับ IT
  (สรุป, แบบทดสอบ 45 ข้อ, ข้อสอบจำลอง, คลังทรัพยากร) ครอบคลุมพีชคณิตเชิงเส้นสัปดาห์ 1–7
- **เตรียมสอบ MFIT ตามโครงข้อสอบจริง** — สรุปเร่งด่วนเรียงตามข้อสอบ 10 ข้อจริง แผนอ่าน
  6 ชั่วโมงตามน้ำหนักคะแนน และชุดฝึกจับเวลา (ปรนัย 20 + คำนวณ 5) รวมคลังข้อสอบเป็น 65 ข้อ
- ฮับ EN-KMITL แบบหลายวิชา พร้อมข้อสอบจำลองเคมีทั่วไป 60 ข้อ และคลังทรัพยากรของตัวเอง
- **สารบัญด้านข้างแบบโต้ตอบ** ในทุกหน้าอ่าน — ไล่ตามตำแหน่งที่อ่าน แถบความคืบหน้า
  ค้นหาหัวข้อ พับ/ขยายได้ และเลื่อนแยกอิสระจากเนื้อหา
- **route เดียว `/courses/[dir]`** รองรับทุกวิชา แทนโครง en-kmitl และ it-kmitl ที่แยกกัน
  คนละต้นไม้ และ route `/pscp` — ฮับห้องแลป Python พร้อมหน้าย่อยแยกตามโมดูล
- นำเข้าสื่อการเรียนปี 1 ทั้งหมดจากคลัง `kmitl-archive` — เพิ่มไฟล์ 507 ไฟล์
  (รวมเป็น 662 ไฟล์ / 1.4 GB) ครอบคลุม 13 รายวิชาทั้งเทอม 1 และเทอม 2
  ตัดไฟล์ซ้ำด้วย content hash และข้ามไฟล์ที่ใหญ่เกิน 20 MB
- **manifest คลังทรัพยากรแบบสร้างอัตโนมัติ** (`lib/library-manifest.json`) —
  ไฟล์ที่วางลง `public/assets/` ตามกติกา path จะขึ้นการ์ดในเว็บทันที
  ไม่ต้องรอเขียน entry มือ เพิ่มการ์ดจาก 173 เป็น 679 ใบ
- script `library:manifest` และ `library:build` สำหรับสร้าง manifest + stats
- `fileType: "file"` สำหรับไฟล์ที่เบราว์เซอร์เปิดไม่ได้ (`.circ` `.xlsx` `.docx`)
  แสดงเป็นการ์ดดาวน์โหลดแทนการ preview
- เอกสาร **[FILE_STRUCTURE.md](./FILE_STRUCTURE.md)** — กติกา path การตั้งชื่อไฟล์
  และ taxonomy กลางภาค/ปลายภาค
- **workflow `_dropzone/`** — วางไฟล์ที่ยังไม่ได้จัดหมวดลงโฟลเดอร์นี้ แล้วชี้ AI agent
  ไปที่ [`docs/DROPZONE_SOP.md`](./docs/DROPZONE_SOP.md) เพื่อให้จัดวิชา/หมวด/ขอบเขตสอบ
  เปลี่ยนชื่อเป็น kebab-case ย้ายเข้าที่ อัปเดต catalog แล้วล้างโฟลเดอร์ให้อัตโนมัติ
  เนื้อหาใน `_dropzone/` ถูก gitignore ไฟล์ที่จัดหมวดไม่ได้จะถูกพักไว้ที่
  `_dropzone/_hold/` พร้อมเหตุผลแทนที่จะเดา
- `content/courses/06016402-IT-Fundamentals/midterm-study-guide.md` และ
  `content/courses/README.md` นำเข้าจาก iLearn
- **พิมพ์เขียวการเรียนรู้ 11 โมดูล** (`lib/spine.ts` — `STANDARD_SPINE`) — สัญญาเดียว
  ว่า "หนึ่งวิชา" ประกอบด้วยอะไรบ้าง ใช้ร่วมกันทุกวิชา แทนที่ track 6 แบบเดิมที่มีอีก
  4 แบบแอบอยู่นอกระบบ ทุกวิชาแสดงครบทั้ง 11 ช่องเสมอ ช่องที่ยังไม่มีเนื้อหาแสดงเป็น
  ช่องล็อกพร้อมบอกว่าขาดอะไร
- `lib/course-bindings.ts` — จุดเดียวที่ประกาศว่าแต่ละวิชาเติมโมดูลไหนได้บ้าง
  เชื่อมกับ spine ผ่าน `lib/course-spine.ts` (`resolveCourseSpine`)
- route เดียว `app/courses/[dir]/[module]/page.tsx` แทนที่ไฟล์หน้าเว็บ 7 ไฟล์ที่
  เกือบซ้ำกันทั้งหมด (`summary` `exam` `mock` `cram` `plan` `analysis` `quiz`
  `library` `labs`) — เพิ่มวิชาใหม่หรือโมดูลใหม่ไม่ต้องแตะ route อีกต่อไป
- ต่อไฟล์ที่ถูกทิ้งไว้ใน `content/courses/*/archive/` เข้า route — โดยเฉพาะ MFIT
  สัปดาห์ 8–15 (เนื้อหาปลายภาคชุดเดียวที่มีอยู่ทั้งระบบ) และ `study-guide-curriculum.json`
  ผ่าน loader ใหม่ `lib/curriculum.ts`
- ฟิลด์ `chapter` บน `SubjectAsset` และ `QuizQuestion` — เชื่อมสไลด์เข้ากับสัปดาห์ที่มัน
  สอน `scripts/build-library-manifest.mjs` อ่านจากชื่อไฟล์ที่ระบุตรงๆ (`week08` `ch3`
  `lec02`) เท่านั้น backfill ได้ 264/670 ไฟล์ พร้อม chip กรองตามบทในคลังทรัพยากร
- **สัญญาคลังข้อสอบที่ตรวจได้ด้วยเครื่อง** (`lib/schemas/content.ts` + `npm run
  content:check`, รันใน `prebuild`) — ตรวจ `sourceRef` ต้องมี, option id ห้ามซ้ำ,
  `quizId` ต้องไม่ชนกัน และอื่นๆ พบหนี้เนื้อหา 223 รายการ (คำอธิบายตัวลวงซ้ำ/กลวง,
  ประกาศสองภาษาปลอม) บันทึกเป็น warning รอซ่อม
- `npm run readiness` — รายงาน Readiness Index (x/11) ของทุกวิชา
- เอกสาร **[docs/LEARNING_BLUEPRINT.md](./docs/LEARNING_BLUEPRINT.md)** — สัญญาเต็มของ
  พิมพ์เขียว 11 โมดูล และ **[Current_Architecture_Report.md](./docs/Current_Architecture_Report.md)**
  รายงานสถาปัตยกรรมที่พิมพ์เขียวนี้อ้างอิง

### ปรับปรุง

- **แท็บช่วงสอบในหน้ารายวิชาเป็นดรอปดาวน์บนมือถือ** — ปุ่มสามตัว
  (ทั้งหมด/ก่อนมิดเทอม/หลังมิดเทอม) พร้อมไอคอนและตัวเลขล้นแถวเดียวบนจอ 360px
  จนข้อความถูกตัดจนอ่านไม่ออก
- **แสดงบัญชีผู้ใช้บนแถบนำทาง** — รูปโปรไฟล์พร้อมชื่อเล่นแทนวงกลมตัวอักษรเปล่า
- นำลิงก์ iJudge ออกจากแถบนำทาง (ยังอยู่ที่ footer)
- **คลังทรัพยากรขึ้นหน้า "เร็วๆ นี้" ชั่วคราว** — ควบคุมด้วยตัวแปรสภาพแวดล้อม
  `NEXT_PUBLIC_LIBRARY_COMING_SOON` ระหว่างจัดหมวดหมู่เอกสารให้เรียบร้อย
- **ออกแบบใหม่** — navbar, footer, splash screen, subject hub และหน้าต่างพรีวิวคลัง
  ทรัพยากร พร้อมคอมโพเนนต์ร่วมชุดใหม่ (course directory, module/track grid,
  การ์ดโมดูล PSCP, lab hub, badge ของแต่ละวิชา)
- **บีบอัด PDF ทุกไฟล์** ผ่านการตรวจสอบภาพก่อน-หลัง (visual regression gate) —
  จาก 1,328 MB เหลือ 908 MB (ลด 32%) ใน 475 จาก 621 ไฟล์ ส่วน 101 ไฟล์ที่ภาพจะเสีย
  ถูกกันไว้ที่ขนาดเดิม
- แปลงหลาย route จาก force-dynamic เป็น static generation เพื่อให้อยู่ในโควตา
  12 serverless function ของ Vercel Hobby
- **เปลี่ยนชื่อไฟล์ใน `public/assets/` ทั้งหมดเป็น kebab-case** (145 ไฟล์) —
  ไฟล์เคมี 11 ไฟล์ที่มีช่องว่างในชื่อไม่ต้อง escape ใน URL อีกต่อไป
- จัดไฟล์เคมีเข้าโฟลเดอร์หมวด (`lectures/` `exercises/` `sheets/`) ให้ทุกวิชา
  ใช้โครง `<namespace>/<subject>/<category>/` เหมือนกันหมด
- ย้าย `ics-midterm-1-2564.pdf` จากรากโฟลเดอร์วิชาเข้า `exams/`
- README ขยายขอบเขตจากเครื่องมือ PSCP เป็นคลังการเรียนรู้ IT KMITL
- CONTRIBUTING เพิ่มหัวข้อเพิ่มรายวิชา เขียนสรุป และอัปโหลดสื่อการเรียน
- **COMPRO แยกข้อสอบจำลอง 60 ข้อออกจากคลังฝึก 10 ข้อ** — เดิมถูกต่อท้ายเงียบๆ ทำให้
  นักศึกษาเจอ 70 ข้อรวดตอนกด "ทำแบบทดสอบ" ตอนนี้แยกเป็นโมดูล `mock_exam` พร้อม
  progress key ของตัวเอง
- URL เก่า `/exam` `/plan` `/analysis` redirect ไปยัง segment ใหม่ (`/mock` `/cram`
  `/overview`) ที่ `next.config.ts`
- sitemap สร้างจาก spine อัตโนมัติแทนรายการ route ที่เขียนมือ
- การ์ดโมดูลในหน้าวิชาเรียงลำดับ **ที่พร้อมใช้ก่อน** ในแต่ละเฟส แทนลำดับตายตัวเดิมที่
  ทำให้การ์ดที่กดได้ปนอยู่กับการ์ดล็อกสีเทา
- **คลังทรัพยากรดึงขึ้นมาเป็นแบนเนอร์เต็มความกว้างด้านบนสุดของหน้าวิชา** พร้อม badge
  สีน้ำเงิน IT KMITL และเอฟเฟกต์ shimmer แทนที่จะเป็นการ์ดขนาดเท่ากันปนอยู่ในกริด
- ตาราง `lib/catalog.ts` เหลือหน้าที่เดียวคือ identity ของวิชา (code/officialCode/
  slug/group) ย้าย `tracks` ไปเป็น binding ใน `lib/course-bindings.ts`
- `FILE_STRUCTURE.md` หัวข้อ 7 เขียนใหม่ทั้งหมดให้ตรงกับ catalog + spine + bindings
- **ซิงก์ข้อมูลโจทย์ OJ และ HTML cache รอบใหม่**

### แก้ไข

- **รวมเนื้อหา EN-KMITL เข้า repo เอง** — deploy จริงไม่มี repo พี่น้องที่โค้ดเคย
  อ้างอิง ทำให้หน้าเพจ 404
- แก้การเรนเดอร์สูตรคณิตศาสตร์ (KaTeX) ใน markdown viewer
- แก้ asset ID ชนกันในตัวสร้าง library manifest
- แก้คำเตือน workspace-root ของ Turbopack และลบ lockfile ที่ไม่ตรงกับตัวจัดการ
  แพ็กเกจของโปรเจกต์อีกต่อไป
- **สูตรคณิตศาสตร์แบบ `$$...$$` หลายบรรทัดทำให้เนื้อหาที่เหลือทั้งไฟล์หายไป** —
  `remark-math` มองว่า `$$` ปิดต้องอยู่บรรทัดของตัวเองล้วนๆ ไฟล์สรุป/ข้อสอบทุกไฟล์
  ในคลังเขียนแบบ LaTeX ปกติ (`$$สูตร$$` ติดเนื้อหาทั้งสองฝั่ง) ทำให้บล็อกหลายบรรทัด
  หาจุดปิดไม่เจอและกลืนทุกอย่างที่เหลือในไฟล์เข้าไปเป็นก้อนเดียว (พบใน
  `study-guide-week08-quiz.md` ของ MFIT ที่หัวข้อ กับ 9 ใน 16 หัวข้อหายไปหลัง
  W08-Q5) แก้ที่ `MdView` จุดเดียวเพราะเป็น renderer ร่วมของทุกหน้า Markdown
- **hydration error จากตำแหน่งแท็ก `<script>` และการซ้อน `<tr>` ที่ไม่ถูกต้อง** —
  โครง DOM ที่เซิร์ฟเวอร์สร้างกับที่เบราว์เซอร์คาดไม่ตรงกัน ทำให้หน้าเพี้ยนตอนโหลด

## [0.5.0] - 17 สิงหาคม 2569

**ผู้ร่วมพัฒนา:** Chatan Petry ([@Jesselpetry](https://github.com/Jesselpetry)), Akarapach Yootsukprasert ([@PGTHEGOD](https://github.com/PGTHEGOD))

### เพิ่ม

- ระบบ **Technique Quiz**: แบบทดสอบ Active-Recall 60 ข้อ สำหรับ 10 โจทย์แนะนำ PSCP พร้อมคำอธิบายเฉลย 2 ภาษา (TH/EN) และบันทึกคะแนนในตัว
- ระบบ **Client-Side Grader**: ตรวจและรันโค้ด Python ด้วย WebAssembly (Pyodide) ภายใน Web Worker แบบ 100% Client-side
- หน้าจอ **Splitter Panel Workspace (`/recommended/[slug]`)**: ปรับหน้าโจทย์แนะนำเป็น Split View ลากปรับขนาดได้ ฝั่งซ้ายเป็น `problem.md` (KaTeX Math) และฝั่งขวาเป็น Code Grader
- ระบบ **Foldable Test Cases**: แสดง Expected Input / Output ของแต่ละ Testcase แบบพับ/ขยายได้ (ค่าเริ่มต้นพับไว้) พร้อมปุ่ม Unfold/Fold All และ Diff View แสดงช่องว่าง (`·`/`↵`)
- ระบบตรวจสอบสไตล์โค้ด **PEP-8 & PSCP Rules**
- คีย์ลัด `Ctrl+Enter` / `Cmd+Enter` สำหรับสั่งรันเทสโค้ดทันที
- **ฮับโจทย์แนะนำ `/recommended`** — คัดโจทย์ 10 ข้อที่ครอบคลุมรูปแบบหลักของวิชา
  แต่ละข้อมีหน้าอ่านโจทย์ คำอธิบายเทคนิค ป้ายเทคนิคประจำข้อ และการ์ดสถิติรวม
  พร้อมปุ่มสลับสถานะของตัวเอง (ยังไม่ทำ / กำลังทำ / ผ่านแล้ว)
- **หน้าอ่านโค้ดเฉลย** — แสดง `main.py` พร้อมไฮไลต์ไวยากรณ์ เลขบรรทัด ปุ่มดาวน์โหลด
  และการ์ด "จุดสำคัญ" สรุปแนวคิดของข้อนั้น
- **ดึงสถิติจริงจาก iJudge เข้าหน้าโจทย์** — จำนวนคนผ่าน อัตราผ่าน และวันหมดเขต
  ของแต่ละข้อมาจากข้อมูลที่ scrape จริง ไม่ใช่ตัวเลขที่พิมพ์ไว้ในโค้ด
- **ตรวจสถานะการ sync กับ GitHub ในหน้าโจทย์แนะนำ** — รู้ว่าข้อไหนมีไฟล์อยู่ใน
  repository ของผู้ใช้แล้ว
- **แบนเนอร์บรีฟวันสอบและชั้นหนังสือประกอบการเรียน** — การ์ดตำรา 2 คอลัมน์
  พร้อมคำอธิบายและปุ่มเปิดอ่าน ย่อ/ขยายรายละเอียดได้
- **URL สั้นระดับบนสุด `/{slug}`** — เปิดโจทย์ด้วยชื่อสั้นได้ พร้อม redirect
  อัตโนมัติจาก URL รูปแบบเดิม
- **หน้า `/pro`** — หน้า landing แยกพร้อม metadata สำหรับ SEO
- **โจทย์ OJ สัปดาห์ 4 และสัปดาห์ 5** (3101–3116) พร้อมสคริปต์ sync สถานะโจทย์
  จาก iJudge

### ปรับปรุง

- ปรับปรุงเลย์เอาต์หน้าโจทย์แนะนำ `/recommended/[slug]` ให้ Fit-to-Screen 100% ของความสูงหน้าจอ (เนื้อหาแต่ละฝั่งเลื่อนแยกกันอย่างอิสระ)
- ปรับปรุงการเรนเดอร์สูตรและสมการคณิตศาสตร์ด้วย KaTeX
- **ประกาศหน้าแรกเปลี่ยนจากแบนเนอร์สัปดาห์ 3 เป็นประกาศ Code Interview**
- **แผงพรีวิวไฟล์เลื่อนไปที่ไฟล์ที่รอ push ให้อัตโนมัติ** — ไม่ต้องไล่หาเองว่าไฟล์ไหน
  กำลังจะถูกส่งขึ้น repository
- **ยกเครื่องการแสดงผลบนมือถือ** — แท็บปรับตามความกว้างจอ ปุ่มมีขนาดพอให้กดด้วยนิ้ว
  และแถบเครื่องมือกะทัดรัดลง

### แก้ไข

- แก้ไขการ Hydrate ข้อมูลแบบ Primitive String ใน `useDraft` ป้องกันข้อผิดพลาด `[object Object]`
- **ป้ายเทคนิคชื่อยาวล้นออกนอกแถว** — ตัดบรรทัดต่อเนื่องได้แล้วทุกขนาดจอ
- **การ์ดสถิติ Learning Log แสดงซ้ำสองใบ** — เหลือใบเดียว และเพิ่มการ์ดรูปแบบหลัก
  ของโจทย์เข้ามาแทนที่ใบที่ซ้ำ
- **คำอธิบายจุดสำคัญ 6 ข้อใน `PROBLEM_TAKEAWAYS` ค้างข้อมูลเก่า** ไม่ตรงกับโจทย์จริง
- **เลย์เอาต์แบบแบ่งครึ่งจอไม่เต็มความกว้างบนมือถือ** — เปลี่ยนไปใช้ตัวแปร CSS
  ที่ปรับตามขนาดจอ

## [0.4.0] - 21 กรกฎาคม 2569

**ผู้ร่วมพัฒนา:** Meaookung144 ([@Meaookung144](https://github.com/Meaookung144)), Chatan Petry ([@Jesselpetry](https://github.com/Jesselpetry))

### เพิ่ม

- ระบบเชื่อมต่อ GitHub (OAuth) และ Push ไฟล์ `submission.md` / `ai_reflection.md` เข้า Repository โดยตรง
- ตัวแก้ไขไฟล์ออนไลน์ (`/repo`) สำหรับแก้ไขและดูตัวอย่างไฟล์ใน Repository ก่อนทำการ Push
- หน้าประวัติเวอร์ชัน (`/version`) พร้อมลิงก์ใน Navbar สำหรับดูประวัติการอัปเดตระบบ
- ส่วนแสดงผล Avatar พร้อม AvatarBadge สำหรับผู้ร่วมพัฒนาในหน้า `/version`
- ไฟล์ `CHANGELOG.md`, `lib/changelog.ts` และแนวทางปฏิบัติตาม Changelog workflow ใน `CONTRIBUTING.md`

### ปรับปรุง

- ปรับปรุง Navbar ให้รองรับอุปกรณ์เคลื่อนที่ (Mobile Responsive) พร้อมเมนู Hamburger
- ปรับปรุง UI ในส่วน GitHub Push และ Wizard ให้แสดงลำดับขั้นตอน (1–10) แบบคลิกได้ พร้อมปุ่มเชื่อมต่อไปยัง GitHub Repo

### แก้ไข

- แก้ไขการทำงานและการแสดงผลของ Folder Component ในหน้าจัดการ Repository

## [0.3.0] - 20 กรกฎาคม 2569

**ผู้ร่วมพัฒนา:** Chatan Petry ([@Jesselpetry](https://github.com/Jesselpetry))

### เพิ่ม

- ระบบสลับธีม (light / dark) พร้อมปุ่ม toggle แบบมี animation

## [0.2.0] - 18 กรกฎาคม 2569

**ผู้ร่วมพัฒนา:** Chatan Petry ([@Jesselpetry](https://github.com/Jesselpetry))

### เพิ่ม

- ประกาศเปิดโจทย์ Week 3

### ปรับปรุง

- ปรับการเรียงลำดับโจทย์และทางลัดประจำสัปดาห์ (`lib/shortcuts.ts`)
- อัปเดตรายการโจทย์ใน `data/oj_problems.json`

## [0.1.1] - 13 กรกฎาคม 2569

**ผู้ร่วมพัฒนา:** Chatan Petry ([@Jesselpetry](https://github.com/Jesselpetry))

### เพิ่ม

- **ต้องยืนยันว่าอ่านคู่มือแล้วก่อนดาวน์โหลดไฟล์** — กันการกดโหลด `submission.md`
  ไปส่งโดยยังไม่ได้อ่านเงื่อนไขการใช้ AI ของวิชา

### แก้ไข

- แก้ไข Path การสร้างไฟล์ของโจทย์ใน Week 2

## [0.1.0] - 10 กรกฎาคม 2569

**ผู้ร่วมพัฒนา:** Chatan Petry ([@Jesselpetry](https://github.com/Jesselpetry))

### เพิ่ม

- ระบบสร้างไฟล์ `submission.md` และ `ai_reflection.md` แบบ Step-by-step
- ระบบคัดลอกและดาวน์โหลดไฟล์
- รองรับโจทย์ Week 1 และ Week 2 ของวิชา PSCP
- **หน้าอ่านเอกสาร AI-Guidelines และหน้าประวัติการใช้งาน** — ชุดเอกสารแนวปฏิบัติ
  การใช้ AI ในวิชา PSCP อ่านได้ในเว็บโดยตรง พร้อมหน้า `/history` ที่เก็บงานที่เคยสร้าง
- **หน้า splash screen ตอนเปิดเว็บ** และเอกสาร `CONTRIBUTING.md` สำหรับผู้ร่วมพัฒนา
- **ตรวจความครบถ้วนของฟอร์มก่อนข้ามขั้น** — กดไปขั้นถัดไปไม่ได้ถ้ายังกรอกไม่ครบ
  แทนที่จะปล่อยให้ได้ไฟล์เปล่าออกไป

### ปรับปรุง

- **ปรับคู่สีทั้งเว็บให้ผ่านมาตรฐาน contrast ระดับ WCAG AA**
- **แถบนำทางและภาพ Open Graph ชุดใหม่แบบความละเอียดสูง**
